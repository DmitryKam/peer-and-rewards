import { makeStyles } from '@mui/styles';

export const useStyle = makeStyles({
  icon: {
    color: '#911b91',
  },
});

export const fabStyle = { backgroundColor: '#f8f8f8', border: '2px solid #911b91' };
