import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  root: { padding: 20 },
  buttonContainer: {
    marginTop: 40,
    display: 'flex',
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
